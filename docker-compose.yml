
services:
#  robot-arm:

  teleshake:
    build: ./teleshake/
    network_mode: host
    environment:
      - PORT=50050

  universal_sila_client:
    image: registry.gitlab.com/sila2/universal-sila-client/sila_universal_client
    extra_hosts:
      - "host.docker.internal:host-gateway"
    network_mode: host
